[00 — MASTER RULES (LEIA ANTES DE USAR QUALQUER PROMPT)]

REGRAS ABSOLUTAS (OBRIGATÓRIAS):
1) Antes de qualquer ação, o Antigravity deve ler por COMPLETO:
   - BRIEFING_PORTFOLIO_NUXT_UI.txt
   - CONVERSA_GEMINI_PORTFOLIO_PARA_ANTIGRAVITY.txt
   - o código do projeto (ROOT onde existe nuxt.config.ts + package.json + app/)
   - references/ (se existir) APENAS como referência, não copiar código direto

2) Identificar no .txt:
   - os erros já cometidos
   - as correções corretas
   - os pontos onde o usuário diz “Gemini errou / você errou / não faça isso”

3) Caminho do projeto:
   - NUNCA assumir caminho.
   - Confirmar ROOT localizando nuxt.config.ts.
   - Reportar o caminho absoluto confirmado.

4) Regra de Etapas:
   - Entregar apenas UM prompt por vez.
   - Parar e aguardar “próximo prompt” para continuar.
   - Prompts de implementação terminam aguardando “Etapa concluída”.

5) Formato obrigatório de resposta (quando for implementação):
   - objetivo da etapa
   - quais arquivos criar/editar (paths)
   - código completo para copiar/colar
   - comandos PowerShell prontos (com cd no ROOT confirmado)
   - como testar no navegador
   - erros comuns e como corrigir

6) Restrições técnicas:
   - Nuxt UI first (evitar libs externas)
   - Não alterar nuxt.config.ts sem explicar
   - Evitar varreduras pesadas (excluir node_modules, .nuxt, .output)
   - Proibido comandos destrutivos (Remove-Item, del recursivo, format, diskpart, etc.)


